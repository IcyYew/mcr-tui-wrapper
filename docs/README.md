# mcr-tui-wrapper
A TUI wrapper prototype for mcrcon to abstract commands into more easily executable commands with persistent state.

A proof of concept prior to a native RCON protocol implementation and ncurses UI.
